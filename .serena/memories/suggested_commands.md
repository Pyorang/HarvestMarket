# Suggested Commands

## System (Windows)
- `dir` — list directory
- `tree /F` — show directory tree with files
- `findstr /S /I "pattern" *.cs` — search in C# files
- `git status` / `git log --oneline` — version control

## Unity
- Open project via Unity Hub
- Build & Run from Unity Editor (File > Build Settings)
- Play mode in Unity Editor for testing

## Notes
- No dedicated CLI build/test/lint commands found (standard Unity workflow)
- No custom editor scripts or CI/CD configs observed
